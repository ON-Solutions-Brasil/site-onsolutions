RewriteEngine On

# Se o arquivo ou diretório existir, entrega direto
RewriteCond %{REQUEST_FILENAME} -f [OR]
RewriteCond %{REQUEST_FILENAME} -d
RewriteRule ^ - [L]

# Envia tudo para o front controller
RewriteRule ^(.*)$ index.php?url=$1 [QSA,L]
